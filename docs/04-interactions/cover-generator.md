# 交互说明：项目封面生成器改版

## 页面结构

```text
固定顶栏
┌────────────────────────────────────────────────────────────┐
│ 返回  封面图生成工具                         JPG  清晰度  下载 │
├───────────────┬────────────────────────────────────────────┤
│ 左侧配置导航   │ 顶部类型选择                             │
│ 标题、方向、资源 │ 画布固定居中，不承担页面滚动               │
└───────────────┴────────────────────────────────────────────┘
```

- 顶栏：返回、页面标题、固定 JPG 输出标识、1x/2x 高清选择、下载。
- 左侧：标题、布局方向和存储标签；颜色、纹理、人物以一级菜单呈现，二级资源面板按需呼出。左侧内容独立滚动，桌面端资源面板浮在侧栏右侧，面板自身有最大高度。
- 中部：顶部只放置封面类型分段控制器，下方为真实 Canvas 预览；原始尺寸显示在画布卡片右上角。预览区固定在工作区内，不因左侧列表变长而移动。

## 信息架构

1. 核心配置：预览区顶部的封面类型、左侧的标题、项目组布局方向和存储标签。
2. 资源菜单：颜色、纹理、人物；点击一级菜单呼出二级面板。
3. 导出：固定 JPG、1x/2x 清晰度和下载。

## 操作清单

| 操作 | 触发 | 逻辑与反馈 |
| --- | --- | --- |
| 切换封面类型 | 点击预览区顶部类型按钮 | 更新字段规则，清空不适用的人物选择；按钮出现选中态；左侧不重复展示该控件 |
| 输入标题 | 输入单行文本 | 150ms 防抖更新预览；不支持 Enter/手动换行，只按当前布局文字框自动换行；显示有效字数与超限提示 |
| 切换布局方向 | 点击 Segmented 方向选项 | 仅项目组中文标题显示；选项同时展示图/文方向图标并更新预览布局 |
| 切换存储标签 | 点击 Switch | 仅项目组可用；更新预览左上角标签 |
| 呼出资源面板 | 点击颜色、纹理或人物一级菜单 | 桌面端从侧栏右侧浮出，移动端在触发项下方内嵌；一次只打开一个面板 |
| 收起资源面板 | 点击空白、再次点击当前一级菜单或按 Esc | 面板收起；按 Esc 后焦点回到触发按钮 |
| 选择背景色 | 点击平铺色板中的具体小色块 | 小色块独立可聚焦和选择；不显示阶数数字；选中只显示明显描边；面板保持打开并更新预览 |
| 选择文字颜色 | 点击颜色面板内的文字颜色选项 | 选中只显示明显描边，不显示勾选图标；面板保持打开并更新预览 |
| 选择纹理 | 点击纹理缩略图 | 选中边框与勾选标记同步；面板保持打开；“无”清空纹理 |
| 切换人物分类 | 点击分类 Tab | 只切换当前资源集合，不清除已经选择的人物，除非封面类型发生变化 |
| 选择人物 | 点击人物缩略图 | 适用人物布局必须保持一个人物选中；选中边框与勾选标记同步，面板保持打开并更新预览 |
| 选择清晰度 | 点击 1x / 2x | 下载时按对应倍率重新渲染 Canvas；导出格式固定为 JPG |
| 下载 | 点击“导出 JPG”按钮 | 字体未完成或导出中禁用；完成后下载 JPG 文件，2x 文件名追加 `@2x` |

## 状态设计

- 默认：所有资源面板收起；预览显示默认色块。
- 字体加载中：左侧显示轻量提示，下载按钮不可用。
- 输入超限：输入框显示错误边框，计数变为错误色；不使用弹窗打断输入。
- 未选择纹理：纹理摘要为“无”，面板显示“无纹理”选项；适用人物布局不提供空人物选项。
- 图片加载失败：资源缩略图显示“加载失败”占位，不影响其余操作。
- 导出中：下载按钮显示“导出中...”并禁用，避免重复下载。
- 无权限：当前演示页没有权限分支；未来接入资源库时补充入口级无权限状态。
- 信息精简：不显示预览标题说明、画布标签、固定比例/格式提示、重复尺寸说明、成功下载提示或“可直接导出”状态；错误与标题校验反馈继续显示。

## 关键交互说明

### 标题与自动换行

- 标题控件使用单行输入框，粘贴或输入产生的 `\r/\n` 会被移除，不将 Enter 解释为强制换行。
- 项目组中文标题按 1–2、3、4、5–6、7–8 字匹配 Figma 组件；中文单字兜底使用 2 字组件。
- 英语特殊标题严格匹配“一个 ASCII 字母 + 一个数字”，或“新 + 一个 ASCII 字母 + 一个数字”，例如 `L1`、`A2`、`z9`、`新L1`、`新A2`。允许首尾空格；内部空格、多位数字和其他前缀均不匹配。渲染时字母统一转为大写。匹配后隐藏布局方向和人物选择；人物配置不参与预览或导出，使用英语专属组件。
- 特殊标题的中文前缀使用 `FZLanTYJW_Da`，字母数字主体使用 `MohrRounded-Heavy`；普通项目组标题使用 `FZLanTYJW_Da`。
- 超出上限仍允许继续输入与下载，但输入框显示错误边框和“超出限制”。

### Figma 组件几何

- 项目封面使用固定人物框 `(54,42,531,300)` 与 280 px 标题样式。
- 项目组中文组件的人物框均为 223×303 px；方向和字数只决定文字框及人物框位置，不能通过用户拖拽改变。
- 「存储」标签始终固定在 `(26,26)`，尺寸 116×57，文案不可编辑；Switch 只控制显示/隐藏。
- 英语组件没有人物素材，使用英语标题字体和背景纹理；不再绘制英文字母装饰背景。

### 背景色选择

- 将现有具体色阶按原有顺序平铺为紧凑色板。
- 每个小色块都是独立的选择按钮，保留色系、阶数和色值的 `aria-label` / `title`。
- 项目封面和项目组封面继续使用所选具体色阶，不增加新的色阶业务字段。
- 色板小色块不显示阶数数字，但在 `aria-label` / `title` 中保留色系、阶数和色值信息。
- 背景色和文字颜色选中状态只通过明显描边表达，不显示勾选图标；资源摘要继续展示当前选择。

### 资源浮层

- 桌面端浮层使用固定定位，贴近触发按钮右侧并限制在视口内，不被侧栏滚动容器裁切。
- 点击空白或按 Esc 关闭；关闭后焦点回到对应一级菜单。
- 选择颜色、文字颜色、纹理或人物后不自动关闭，支持连续试选。
- 900px 以下使用内嵌面板，面板宽度不超过侧栏内容区，不产生横向滚动。

### 资源选择

- 纹理和人物面板采用分类 Tab + 固定最大高度网格；人物分类 Tab 超出面板宽度时支持横向循环滚动。
- 分类 Tab 隐藏原生滚动条，但保留横向触摸、触摸板和滚轮滚动能力；滚动到任一端后继续同方向滚动会无缝衔接到另一端，不自动播放。
- 分类 Tab 的循环衔接只服务于视觉滚动，不创建重复分类；辅助技术只读取一组真实分类，键盘焦点不落到虚拟节点上。
- 网格使用自适应列数，素材数量增加时只在面板内部滚动。
- 人物网格不提供“无人像”选项；普通项目/项目组中文标题默认选中当前分类的首个人物，用户只能在人物素材之间切换。
- 缩略图只承担识别和选择，不显示长名称；完整名称通过 `title` 和 `aria-label` 提供。
- 资源图片加载失败时保留按钮位置，避免布局跳动。

### 导出反馈

- 页面只显示固定的 JPG 输出状态，不提供格式切换。
- 下载按钮文案为“导出 JPG”，显示主操作层级；清晰度使用次级分段选择；1x/2x 都按对应物理像素重新渲染。
- 下载完成不额外增加弹窗或 toast，保持当前演示页的轻量行为；若后续接入真实接口，再补充成功与失败反馈。

### 预览区顶部与方向控制

- 封面类型控制放在预览工作区顶部，使用两个紧凑分段选项；可见文字水平居中，每个选项保留尺寸信息到可访问名称，尺寸同时显示在画布卡片右上角。
- 预览工作区不展示“可直接导出”状态；下载按钮继续使用禁用态和 title 提示表达不可导出的原因。
- 图文方向使用 Segmented 分段控制器；“左图右文”显示 `Image` 在左、`Type` 在右，“左文右图”反向排列。图标复用项目已有的 Lucide 图标，不新增依赖。
- 侧栏桌面端约 320px，保留标题、方向、存储和资源入口的可读性；视口收窄时按现有断点继续响应式缩放。

## 组件复用建议

- 复用现有 `Switch` 组件处理存储标签。
- 复用现有 `CoverPreview`、`drawCover`、`layoutMatcher` 和资源 token，不改变画布渲染职责。
- 按钮、输入、圆角、品牌色和密度沿用 `docs/01-foundations/` 与 `docs/02-components/`。
- `ColorPicker` 继续作为颜色选择组件，调整为紧凑平铺色板；具体色阶按钮仍可直接选择，视觉上不显示阶数数字或勾选图标。
- `TextColorPicker` 复用相同的描边选中规则，不显示勾选图标。
- `ThumbnailGrid` 继续作为纹理和人物选择组件，但增加分类切换、循环分类导航、固定高度和加载失败占位。

## 研发注意事项

- 桌面端工作区使用 `height: 100dvh` 与 `overflow: hidden`，仅左侧配置容器允许 `overflow-y: auto`。
- 900px 以下切换为单列：左侧配置先展示，资源浮层改为内嵌，预览区使用独立固定高度；不得出现横向溢出。
- 保留 `prefers-reduced-motion` 下的静态状态，交互过渡只使用 opacity、transform 或颜色。
- 所有图标按钮必须有 `aria-label`；颜色系、缩略图和清晰度控件必须表达 `aria-pressed` 或等效选中语义。
- 不引入新依赖，不修改全局 Vite 配置、路由、接口或数据库。

## 待乔布斯确认的问题

- 当前版本沿用“标题超限仍可下载”的旧行为。若业务希望强制拦截，需要在下一轮单独确认。
- 后续素材数量预计会增长，但素材搜索、收藏、分页和权限不在本次改版范围内。
- 项目组背景色应以需求规定的第 1 阶为准，还是以 Figma 示例中的第 4 阶为准？
- 项目组纹理应使用需求规定的 35% 铺满，还是 Figma 示例的低透明度平铺/混合模式？
- 项目组标题颜色采用 Figma 的白色，还是按背景色阶自动匹配？
- 项目封面纹理以需求的 contain 规则为准，还是以 Figma 示例的铺满画布为准？
