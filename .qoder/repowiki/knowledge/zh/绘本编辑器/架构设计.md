- 采用 Vite + React + TypeScript 技术栈，通过 `src/main.tsx` 挂载根组件。
- 状态管理使用 Zustand 并集成 `persist` 中间件实现数据本地持久化，核心逻辑集中在 `useBookStore`。
- UI 架构遵循三栏式布局（`EditorLayout`），包含左侧页面缩略图导航、中央画布编辑区及右侧属性设置面板。
- 依赖 `@dnd-kit` 库实现页面的拖拽排序功能，通过 `types/book.ts` 统一定义绘本、页面及样式的数据结构。